<!DOCTYPE html>
<html>
<head>
    <title>Carrito de Compras</title>
</head>
<body>
    <h1>Carrito de Compras</h1>
    
    <table>
        <thead>
            <tr>
                <th>TICKET</th>
                <th>PRECIO</th>
                <th>SELECCIONA CANTIDAD</th>
            </tr>
        </thead>
        <tbody>
            <!-- Iterar sobre los elementos del carrito -->
            {% for item in carrito %}
            <tr>
                <td>{{ item.evento.nombre }}</td>
                <td>{{ item.evento.precio }}</td>
                <td>
                    <input type="number" name="cantidad" value="{{ item.cantidad }}">
                    <!-- Puedes agregar botones "+" y "-" para aumentar/disminuir la cantidad -->
                    <button onclick="decrementarCantidad({{ item.id }})">-</button>
                    <button onclick="incrementarCantidad({{ item.id }})">+</button>
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    
    <p>Total: {{ total }}</p>
    
    <!-- Puedes agregar un botón para eliminar un evento del carrito -->
    <button onclick="eliminarEvento(evento_id)">Eliminar Evento</button>
    
    <!-- Función para mostrar el resumen de las entradas seleccionadas -->
    <h2>RESUMEN</h2>
    <p>{{ total_entradas }} Entradas Seleccionadas</p>
    <table>
        <thead>
            <tr>
                <th>TICKET</th>
                <th>PRECIO</th>
                <th>Eliminar</th>
            </tr>
        </thead>
        <tbody>
            <!-- Iterar sobre los elementos del carrito -->
            {% for item in carrito %}
            <tr>
                <td>{{ item.evento.nombre }}</td>
                <td>{{ item.evento.precio }}</td>
                <td><button onclick="eliminarEvento(item.id)">Eliminar</button></td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    
    <p>TOTAL: {{ total }}</p>
    
    <script>
        // Función para incrementar la cantidad de un evento en el carrito
        function incrementarCantidad(evento_id) {
            // Realizar la lógica para incrementar la cantidad del evento en el carrito
        }

        // Función para decrementar la cantidad de un evento en el carrito
        function decrementarCantidad(evento_id) {
            // Realizar la lógica para decrementar la cantidad del evento en el carrito
        }

        // Función para eliminar un evento del carrito
        function eliminarEvento(evento_id) {
            // Realizar la lógica para eliminar el evento del carrito
        }
    </script>
</body>
</html>
